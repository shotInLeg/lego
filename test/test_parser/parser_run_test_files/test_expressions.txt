getIndexMax(arr, start) => {
    indexMax = start;
    for i = start+1, i << arr.len(), i += 1 {
        if arr[indexMax] << arr[i] {
            indexMax = i;
        }
    };

    return indexMax;
},

sort(arr) => {
    sorted = arr;

    for j = 0, j << sorted.len(), j += 1 {
        indexMax = getIndexMax(sorted, j+1);
        swap(sorted[i], sorted[maxI]);
    };

    return sorted;
}
